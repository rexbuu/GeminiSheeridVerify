# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Port for health check (Render will set this automatically)
PORT=8080

# ──────────────────────────────────────────
# PROXIES (Optional)
# ──────────────────────────────────────────
# OPTION 1: Use proxies.json file (Local Dev)
# Copy proxies.example.json → proxies.json
#
# OPTION 2: Use Environment Variable (Render/Cloud)
# Paste your JSON array of proxies here as a single string
# Example: ["socks5://user:pass@host:1080", "http://..."]
# PROXIES_JSON=[]

# ──────────────────────────────────────────
# ADMIN PANEL (Optional)
# ──────────────────────────────────────────
# Password to access /admin panel for managing redeem codes
# Default: Mrn00btnw (change this for production!)
# ADMIN_PASSWORD=your_secure_password

# Secret URL path for admin panel (no one can guess it!)
# Default: admin (e.g., /admin). Set to something random like "xyz123secret"
# ADMIN_ROUTE=your_secret_route

# Flask session secret key (set this to keep admin logged in across restarts)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# FLASK_SECRET_KEY=your_64_char_hex_string

# ──────────────────────────────────────────
# KEEP ALIVE (Render Free Tier)
# ──────────────────────────────────────────
# Your Render service URL - the bot will ping itself every 5 minutes
# to prevent Render from putting it to sleep
# Example: https://your-app.onrender.com
# KEEP_ALIVE_URL=https://your-app.onrender.com
